# TODO - حل جميع المشاكل والثغرات - مشروع Musketeers

## الهدف
ضمان أن جميع الكود والملفات تعمل ومرتبطة بالنظام ككل كجهاز عصبي قوي ومستقر وآمن وسريع وسهل الاستخدام، بدون مشاكل نهائية أو ثغرات تمنع التطوير المستقبلي.

## الأولوية الحرجة (Critical) - يجب حلها فوراً

### 1. إصلاح تنفيذ Fetch في Mailbox
**الحالة**: ❌ لم يتم الإصلاح
**الموقع**: `pkg/mailbox/mailbox.go` lines 88-95
**الأولوية**: حرجة
**التأثير**: وظيفة الاتصال الأساسية معطلة

**الخطوات**:
- [ ] إضافة طريقة `ListKeys` إلى واجهة BlockStore في `pkg/content/store.go`
- [ ] تنفيذ `ListKeys` في BadgerBlockStore في `pkg/content/badger.go`
- [ ] تنفيذ `ListKeys` في MemoryBlockStore في `pkg/content/memory.go`
- [ ] تنفيذ Fetch الصحيح في Mailbox باستخدام ListKeys
- [ ] إضافة اختبارات للوظيفة الجديدة
- [ ] اختبار التكامل مع نظام البريد الموجود

**التنفيذ المتوقع**:
```go
// إضافة إلى BlockStore interface
ListKeys(prefix string) ([]string, error)

// تنفيذ في BadgerBlockStore
func (s *BadgerBlockStore) ListKeys(prefix string) ([]string, error) {
    var keys []string
    err := s.db.View(func(txn *badger.Txn) error {
        iterator := txn.NewIterator(badger.DefaultIteratorOptions)
        defer iterator.Close()
        
        prefixBytes := s.blockKey(prefix)
        for iterator.Seek(prefixBytes); iterator.ValidForPrefix(prefixBytes); iterator.Next() {
            item := iterator.Item()
            key := item.Key()
            keys = append(keys, string(key[len(s.prefix):]))
        }
        return nil
    })
    return keys, err
}

// تنفيذ Fetch الصحيح في Mailbox
func (m *Mailbox) Fetch(recipientDID string, recipientPrivKey []byte) ([]*Message, error) {
    prefix := recipientDID + ":"
    keys, err := m.store.ListKeys(prefix)
    if err != nil {
        return nil, fmt.Errorf("failed to list messages: %w", err)
    }
    
    var messages []*Message
    for _, key := range keys {
        data, err := m.store.Get(key)
        if err != nil {
            continue
        }
        
        var msg Message
        if err := json.Unmarshal(data, &msg); err != nil {
            continue
        }
        
        // فك تشفير الرسالة
        decrypted, err := m.decrypt(msg.EncryptedContent, recipientPrivKey)
        if err != nil {
            continue
        }
        
        msg.Content = decrypted
        messages = append(messages, &msg)
    }
    
    return messages, nil
}
```

**التحقق**:
- [ ] اختبار استرجاع الرسائل
- [ ] اختبار فك التشفير
- [ ] اختبار التكامل مع EmailManager
- [ ] اختبار الأداء مع عدد كبير من الرسائل

---

### 2. تنفيذ حزمة telemetry أو إزالة المرجع
**الحالة**: ❌ لم يتم الإصلاح
**الموقع**: `pkg/telemetry/` (غير موجود)
**الأولوية**: حرجة
**التأثير**: مرجع لحزمة غير موجودة

**الخطوات**:
- [ ] البحث عن جميع المراجع إلى pkg/telemetry في الكود
- [ ] إزالة جميع المراجع إذا الحزمة غير مطلوبة
- [ ] أو إنشاء حزمة telemetry كاملة إذا مطلوبة

**التنفيذ المتوقع**:
```go
// إذا تم إنشاء الحزمة:
package telemetry

import (
    "time"
)

// MetricsCollector يجمع مقاييس النظام
type MetricsCollector struct {
    metrics map[string]interface{}
    mu      sync.RWMutex
}

func NewMetricsCollector() *MetricsCollector {
    return &MetricsCollector{
        metrics: make(map[string]interface{}),
    }
}

func (mc *MetricsCollector) RecordMetric(name string, value interface{}) {
    mc.mu.Lock()
    defer mc.mu.Unlock()
    mc.metrics[name] = value
}

func (mc *MetricsCollector) GetMetric(name string) (interface{}, bool) {
    mc.mu.RLock()
    defer mc.mu.RUnlock()
    val, exists := mc.metrics[name]
    return val, exists
}
```

**التحقق**:
- [ ] البحث عن المراجع باستخدام grep
- [ ] إزالة أو إنشاء الحزمة
- [ ] اختبار أن الكود يعمل بدون المرجع

---

## الأولوية العالية (High) - يجب حلها قريباً

### 3. دمج الحزم المعزولة أو توثيقها كحزم اختيارية
**الحالة**: ❌ لم يتم الإصلاح
**المواقع**: 11 حزم معزولة
**الأولوية**: عالية
**التأثير**: كود غير مستخدم، إمكانات ضائعة

**الحزم المعزولة**:
- [ ] pkg/analytics/ - دمج مع نظام المراقبة
- [ ] pkg/backup/ - دمج مع نظام التخزين
- [ ] pkg/ledger/ - دمج مع نظام المدفوعات
- [ ] pkg/notifications/ - دمج مع نظام الإيميل
- [ ] pkg/plugins/ - دمج مع نظام الوكلاء
- [ ] pkg/sandbox/ - دمج مع نظام الوكلاء
- [ ] pkg/sdk/ - دمج مع نظام التكامل
- [ ] pkg/search/ - دمج مع نظام التخزين
- [ ] pkg/upgrade/ - دمج مع نظام النشر
- [ ] pkg/acp/ - دمج مع نظام الاتصال

**الخطوات لكل حزمة**:
- [ ] مراجعة وظائف الحزمة
- [ ] تحديد المكان المناسب للدمج
- [ ] تنفيذ التكامل
- [ ] إضافة اختبارات
- [ ] تحديث التوثيق
- [ ] أو توثيق كحزمة اختيارية

**التحقق**:
- [ ] اختبار التكامل
- [ ] اختبار الأداء
- [ ] مراجعة التوثيق

---

### 4. زيادة تغطية الاختبارات إلى >80%
**الحالة**: ❌ لم يتم الإصلاح
**الموقع**: جميع الحزم
**الأولوية**: عالية
**التأثير**: جودة الكود، الثبات

**الخطوات**:
- [ ] قياس التغطية الحالية باستخدام go test -cover
- [ ] تحديد الحزم ذات التغطية المنخفضة
- [ ] كتابة اختبارات للحزم الحرجة أولاً
- [ ] كتابة اختبارات للحزم العالية
- [ ] كتابة اختبارات للحزم المتوسطة
- [ ] كتابة اختبارات للحزم المنخفضة
- [ ] إضافة اختبارات التكامل
- [ ] إضافة اختبارات E2E

**الهدف**: >80% تغطية لجميع الحزم

**التحقق**:
- [ ] قياس التغطية النهائية
- [ ] التأكد من أن جميع الاختبارات تمر
- [ ] مراجعة جودة الاختبارات

---

## الأولوية المتوسطة (Medium) - يجب حلها في المستقبل القريب

### 5. إضافة دعم ملف تكوين (YAML/TOML)
**الحالة**: ❌ لم يتم الإصلاح
**الموقع**: النظام كله
**الأولوية**: متوسطة
**التأثير**: سهولة الإدارة

**الخطوات**:
- [ ] تصميل هيكل ملف التكوين
- [ ] اختيار صيغة (YAML أو TOML)
- [ ] إنشاء حزمة config
- [ ] تنفيذ قراءة ملف التكوين
- [ ] تنفيذ التحقق من صحة التكوين
- [ ] تحديث جميع المكونات لاستخدام التكوين
- [ ] إضافة ملف تكوين مثال
- [ ] توثيق خيارات التكوين

**التنفيذ المتوقع**:
```go
// config/config.go
package config

import (
    "os"
    "gopkg.in/yaml.v3"
)

type Config struct {
    Server   ServerConfig   `yaml:"server"`
    Database DatabaseConfig `yaml:"database"`
    Email    EmailConfig    `yaml:"email"`
    Storage  StorageConfig  `yaml:"storage"`
}

func LoadConfig(path string) (*Config, error) {
    data, err := os.ReadFile(path)
    if err != nil {
        return nil, err
    }
    
    var config Config
    err = yaml.Unmarshal(data, &config)
    if err != nil {
        return nil, err
    }
    
    return &config, nil
}
```

**التحقق**:
- [ ] اختبار قراءة ملف التكوين
- [ ] اختبار التحقق من الصحة
- [ ] اختبار التكامل مع المكونات

---

### 6. توحيد مكتبة التسجيل (logrus أو zap)
**الحالة**: ❌ لم يتم الإصلاح
**الموقع**: جميع الحزم
**الأولوية**: متوسطة
**التأثير**: الاتساق

**الخطوات**:
- [ ] اختيار مكتبة واحدة (zap موصى به للأداء)
- [ ] البحث عن جميع استخدامات logrus
- [ ] استبدال logrus بـ zap
- [ ] تحديث جميع الحزم
- [ ] توحيد تنسيق السجلات
- [ ] تحديث التوثيق

**التحقق**:
- [ ] اختبار أن جميع السجلات تعمل
- [ ] مراجعة تنسيق السجلات
- [ ] اختبار الأداء

---

### 7. توحيد أنماط معالجة الأخطاء
**الحالة**: ❌ لم يتم الإصلاح
**الموقع**: جميع الحزم
**الأولوية**: متوسطة
**التأثير**: الاتساق، سهولة التصحيح

**الخطوات**:
- [ ] مراجعة أنماط معالجة الأخطاء الحالية
- [ ] تحديد أنماط غير متسقة
- [ ] توحيد استخدام fmt.Errorf مع %w
- [ ] إضافة سياق مفصل للأخطاء
- [ ] تحديث جميع الحزم
- [ ] إضافة اختبارات للأخطاء

**التحقق**:
- [ ] مراجعة أنماط الأخطاء
- [ ] اختبار معالجة الأخطاء
- [ ] اختبار سلاسل الأخطاء

---

### 8. إضافة التحقق الشامل من المدخلات
**الحالة**: ❌ لم يتم الإصلاح
**الموقع**: جميع واجهات برمجة التطبيقات العامة
**الأولوية**: متوسطة
**التأثير**: الأمان، الاستقرار

**الخطوات**:
- [ ] تحديد جميع واجهات برمجة التطبيقات العامة
- [ ] إضافة التحقق من المدخلات لكل واجهة
- [ ] التحقق من السلاسل الفارغة
- [ ] التحقق من المؤشرات nil
- [ ] التحقق من النطاقات (ranges)
- [ ] التحقق من الأنواع
- [ ] إضافة رسائل خطأ واضحة
- [ ] إضافة اختبارات للتحقق

**التحقق**:
- [ ] اختبار المدخلات الصحيحة
- [ ] اختبار المدخلات غير الصحيحة
- [ ] اختبار رسائل الخطأ

---

## الأولوية المنخفضة (Low) - يمكن حلها لاحقاً

### 9. ترجمة التعليقات العربية إلى الإنجليزية
**الحالة**: ❌ لم يتم الإصلاح
**الموقع**: جميع أنحاء الكود
**الأولوية**: منخفضة
**التأثير**: القابلية للصيانة

**الخطوات**:
- [ ] البحث عن جميع التعليقات العربية
- [ ] ترجمة التعليقات إلى الإنجليزية
- [ ] الحفاظ على المعنى الأصلي
- [ ] مراجعة الترجمات
- [ ] تحديث التوثيق

**التحقق**:
- [ ] مراجعة التعليقات المترجمة
- [ ] التأكد من الدقة

---

### 10. إضافة مهلات (timeouts) للعمليات
**الحالة**: ❌ لم يتم الإصلاح
**الموقع**: جميع العمليات الخارجية
**الأولوية**: منخفضة
**التأثير**: الاستقرار، الأداء

**الخطوات**:
- [ ] تحديد جميع العمليات الخارجية
- [ ] إضافة context مع timeout
- [ ] إضافة مهلات افتراضية
- [ ] إضافة اختبارات للمهلات
- [ ] توثيق المهلات

**التحقق**:
- [ ] اختبار المهلات
- [ ] اختبار انتهاء المهلات

---

### 11. إضافة حدود الموارد (resource limits)
**الحالة**: ❌ لم يتم الإصلاح
**الموقع**: جميع العمليات كثيفة الموارد
**الأولوية**: منخفضة
**التأثير**: الاستقرار، الأداء

**الخطوات**:
- [ ] تحديد العمليات كثيفة الموارد
- [ ] إضافة حدود الذاكرة
- [ ] إضافة حدود CPU
- [ ] إضافة حدود goroutines
- [ ] إضافة اختبارات الحدود
- [ ] توثيق الحدود

**التحقق**:
- [ ] اختبار الحدود
- [ ] اختبار تجاوز الحدود

---

### 12. إضافة إدارة الذاكرة (memory management)
**الحالة**: ❌ لم يتم الإصلاح
**الموقع**: جميع العمليات التي تستهلك ذاكرة
**الأولوية**: منخفضة
**التأثير**: الاستقرار، الأداء

**الخطوات**:
- [ ] تحديد العمليات التي تستهلك ذاكرة
- [ ] إضافة تجمعات الذاكرة (pools)
- [ ] إضافة تحرير الذاكرة
- [ ] إضافة اختبارات الذاكرة
- [ ] توثيق إدارة الذاكرة

**التحقق**:
- [ ] اختبار إدارة الذاكرة
- [ ] اختبار تسريبات الذاكرة

---

### 13. إضافة إدارة الاتصالات (connection management)
**الحالة**: ❌ لم يتم الإصلاح
**الموقع**: جميع الاتصالات الخارجية
**الأولوية**: منخفضة
**التأثير**: الاستقرار، الأداء

**الخطوات**:
- [ ] تحديد جميع الاتصالات الخارجية
- [ ] إضافة تجمعات الاتصالات (pools)
- [ ] إعادة استخدام الاتصالات
- [ ] إضافة مهلات الاتصالات
- [ ] إضافة اختبارات الاتصالات
- [ ] توثيق إدارة الاتصالات

**التحقق**:
- [ ] اختبار إدارة الاتصالات
- [ ] اختبار فشل الاتصالات

---

### 14. إضافة معالجة الأخطاء المتقدمة (advanced error handling)
**الحالة**: ❌ لم يتم الإصلاح
**الموقع**: جميع الحزم
**الأولوية**: منخفضة
**التأثير**: سهولة التصحيح

**الخطوات**:
- [ ] إضافة أنواع أخطاء مخصصة
- [ ] إضافة سياق مفصل للأخطاء
- [ ] إضافة stack traces
- [ ] إضافة logging للأخطاء
- [ ] إضافة metrics للأخطاء
- [ ] توثيق معالجة الأخطاء

**التحقق**:
- [ ] اختبار معالجة الأخطاء
- [ ] اختبار stack traces

---

### 15. إضافة مراقبة الأداء (performance monitoring)
**الحالة**: ❌ لم يتم الإصلاح
**الموقع**: النظام كله
**الأولوية**: منخفضة
**التأثير**: الأداء، المراقبة

**الخطوات**:
- [ ] إضافة metrics للأداء
- [ ] إضافة profiling
- [ ] إضافة tracing
- [ ] إضافة dashboards
- [ ] إضافة alerts
- [ ] توثيق المراقبة

**التحقق**:
- [ ] اختبار المراقبة
- [ ] اختبار alerts

---

### 16. إضافة اختبارات الأداء (performance tests)
**الحالة**: ❌ لم يتم الإصلاح
**الموقع**: جميع الحزم الحرجة والعالية
**الأولوية**: منخفضة
**التأثير**: الأداء

**الخطوات**:
- [ ] إضافة benchmark tests
- [ ] إضافة load tests
- [ ] إضافة stress tests
- [ ] توثيق نتائج الأداء
- [ ] إضافة أهداف الأداء

**التحقق**:
- [ ] تشغيل benchmark tests
- [ ] مراجعة نتائج الأداء

---

### 17. إضافة اختبارات الأمان (security tests)
**الحالة**: ❌ لم يتم الإصلاح
**الموقع**: جميع الحزم التي تتعامل مع بيانات حساسة
**الأولوية**: منخفضة
**التأثير**: الأمان

**الخطوات**:
- [ ] إضافة اختبات التشفير
- [ ] إضافة اختبات المصادقة
- [ ] إضافة اختبات التفويض
- [ ] إضافة اختبارات حقن SQL
- [ ] إضافة اختبارات XSS
- [ ] توثيق اختبارات الأمان

**التحقق**:
- [ ] تشغيل اختبارات الأمان
- [ ] مراجعة نتائج الأمان

---

### 18. إضافة اختبارات التزامن (concurrency tests)
**الحالة**: ❌ لم يتم الإصلاح
**الموقع**: جميع الحزم التي تستخدم goroutines
**الأولوية**: منخفضة
**التأثير**: الاستقرار

**الخطوات**:
- [ ] إضافة race detector tests
- [ ] إضافة اختبارات deadlocks
- [ ] إضافة اختبارات goroutine leaks
- [ ] توثيق اختبارات التزامن

**التحقق**:
- [ ] تشغيل race detector
- [ ] مراجعة نتائج التزامن

---

### 19. إضافة اختبارات التكامل (integration tests)
**الحالة**: ❌ لم يتم الإصلاح
**الموقع**: جميع المكونات الرئيسية
**الأولوية**: منخفضة
**التأثير**: الثبات

**الخطوات**:
- [ ] إضافة اختبارات تكامل البريد
- [ ] إضافة اختبارات تكامل التخزين
- [ ] إضافة اختبارات تكامل الوكلاء
- [ ] إضافة اختبارات تكامل الجلسات
- [ ] توثيق اختبارات التكامل

**التحقق**:
- [ ] تشغيل اختبارات التكامل
- [ ] مراجعة نتائج التكامل

---

### 20. إضافة اختبارات E2E (end-to-end tests)
**الحالة**: ❌ لم يتم الإصلاح
**الموقع**: سيرات العمل الرئيسية
**الأولوية**: منخفضة
**التأثير**: الثبات

**الخطوات**:
- [ ] إضافة اختبارات E2E للهوية
- [ ] إضافة اختبارات E2E للبريد
- [ ] إضافة اختبارات E2E للتخزين
- [ ] إضافة اختبارات E2E للوكلاء
- [ ] توثيق اختبارات E2E

**التحقق**:
- [ ] تشغيل اختبارات E2E
- [ ] مراجعة نتائج E2E

---

## التحقق النهائي (Final Verification)

### التحقق من عدم وجود مشاكل نهائية:

#### التحقق من جميع الملفات:
- [ ] التأكد من أن جميع الملفات Go تُجمع بدون أخطاء
- [ ] التأكد من عدم وجود أخطاء استيراد
- [ ] التأكد من عدم وجود أخطاء تجميع
- [ ] التأكد من عدم وجود أخطاء linting حرجة

#### التحقق من جميع الحزم:
- [ ] التأكد من أن جميع الحزم لها تبعيات
- [ ] التأكد من عدم وجود حزم معزولة (أو موثقة)
- [ ] التأكد من أن جميع الحزم لها اختبارات
- [ ] التأكد من أن جميع الحزم لها توثيق

#### التحقق من جميع الوظائف:
- [ ] اختبار إنشاء الهوية
- [ ] اختبار التشفير وفك التشفير
- [ ] اختبار إرسال واستقبال البريد
- [ ] اختبار تخزين واسترجاع الملفات
- [ ] اختبار تسجيل وإدارة الوكلاء
- [ ] اختبار إنشاء وإدارة الجلسات
- [ ] اختبار الاتصال P2P
- [ ] اختبار نظام الأحداث

#### التحقق من الأمان:
- [ ] اختبار التشفير (Ed25519, AES-256-GCM, scrypt)
- [ ] اختبار التحقق من التوقيعات
- [ ] اختبار التحقق من الهوية
- [ ] اختبار سياسات الوصول
- [ ] اختبار عزل البيانات
- [ ] اختبار أمان الاتصالات

#### التحقق من الأداء:
- [ ] اختبار سرعة التشفير
- [ ] اختبار سرعة التخزين
- [ ] اختبار سرعة الاتصال P2P
- [ ] اختبار سرعة معالجة الأحداث
- [ ] اختبار استخدام الذاكرة
- [ ] اختبار استخدام CPU

#### التحقق من الاستقرار:
- [ ] اختبار إعادة التشغيل
- [ ] اختبار التعافي من الأخطاء
- [ ] اختبار إدارة الاتصالات
- [ ] اختبار إدارة الذاكرة
- [ ] اختبار إدارة goroutines

#### التحقق من سهولة الاستخدام:
- [ ] مراجعة واجهات برمجة التطبيقات
- [ ] مراجعة التوثيق
- [ ] مراجعة الأمثلة
- [ ] اختبار سيناريوهات المستخدم

---

## خطة التنفيذ الزمنية

### الأسبوع 1 (الأولوية الحرجة):
- [ ] إصلاح تنفيذ Fetch في Mailbox
- [ ] تنفيذ حزمة telemetry أو إزالة المرجع

### الأسبوع 2-3 (الأولوية العالية):
- [ ] دمج الحزم المعزولة (5 حزم)
- [ ] زيادة تغطية الاختبارات إلى >80%

### الأسبوع 4-6 (الأولوية المتوسطة):
- [ ] إضافة دعم ملف تكوين
- [ ] توحيد مكتبة التسجيل
- [ ] توحيد أنماط معالجة الأخطاء
- [ ] إضافة التحقق الشامل من المدخلات

### الأسبوع 7-10 (الأولوية المنخفضة):
- [ ] ترجمة التعليقات العربية
- [ ] إضافة مهلات
- [ ] إضافة حدود الموارد
- [ ] إضافة اختبارات الأداء والأمان والتزامن

### الأسبوع 11-12 (التحقق النهائي):
- [ ] التحقق من جميع الملفات
- [ ] التحقق من جميع الحزم
- [ ] التحقق من جميع الوظائف
- [ ] التحقق من الأمان والأداء والاستقرار

---

## معايير النجاح

### معايير الجودة:
- ✅ جميع الملفات تُجمع بدون أخطاء
- ✅ جميع الحزم لها تبعيات أو موثقة
- ✅ تغطية الاختبارات >80%
- ✅ لا توجد مشاكل compile-time
- ✅ لا توجد مشاكل linting حرجة

### معايير الأمان:
- ✅ جميع البيانات مشفرة
- ✅ جميع الاتصالات آمنة
- ✅ جميع الهويات موثقة
- ✅ جميع السياسات مُنفذة
- ✅ لا توجد ثغرات أمنية معروفة

### معايير الأداء:
- ✅ التشفير سريع (<100ms)
- ✅ التخزين سريع (<50ms)
- ✅ الاتصال P2P سريع (<200ms)
- ✅ معالجة الأحداث سريعة (<10ms)
- ✅ استخدام الذاكرة محدود (<1GB)

### معايير الاستقرار:
- ✅ إعادة التشغيل تعمل
- ✅ التعافي من الأخطاء يعمل
- ✅ إدارة الاتصالات تعمل
- ✅ إدارة الذاكرة تعمل
- ✅ لا توجد goroutine leaks

### معايير سهولة الاستخدام:
- ✅ واجهات برمجة التطبيقات واضحة
- ✅ التوثيق شامل
- ✅ أمثلة كافية
- ✅ سيناريوهات المستخدم مُختبرة

---

## الخلاصة

هذا TODO شامل لحل جميع المشاكل والثغرات المكتشفة في COMPREHENSIVE_SUMMARY.md، مهما كانت صغيرة. الهدف هو ضمان أن جميع الكود والملفات تعمل ومرتبطة بالنظام ككل كجهاز عصبي قوي ومستقر وآمن وسريع وسهل الاستخدام، بدون مشاكل نهائية أو ثغرات تمنع التطوير المستقبلي.

**المشاكل المكتشفة**: 22 مشكلة
**المشاكل المُصلحة**: 1
**المشاكل المتبقية**: 21
**الخطوات المطلوبة**: 100+ خطوة
**المدة المتوقعة**: 12 أسبوع

بإكمال جميع الخطوات في هذا TODO، سيكون المشروع جاهزاً للتطوير المستقبلي بثقة تامة.
