# Musketeers Platform — Complete Map

> Generated: 2026-06-24 | Go 1.25.3 | ~365 .go files | 60+ packages
> 
> ✅ = Working | ⚠️ = Minor issue | 🔴 = Bug | 📦 = Package | 🔗 = Depends on | 🐛 = Bug
> 
> ## 🎯 VISION
> **Decentralized AI Studio** (like Cursor, for ALL LLMs & Agents). Built by code agent "Devon".
> **Session is the workspace** — human client sees ALL agents/models collaborating in real-time.
> Agents run locally + connect via TCP bridge. Shared memory, skills, workflows.
> External integration via MCP, webhooks, P2P email, HTTP gateway for .mskt domains.

---

## LAYER 0 — Foundation (no internal dependencies)

### 📦 `pkg/common` — Common Interfaces
- **Files**: `interfaces.go`, `interfaces_test.go`
- **Purpose**: KeyResolver, DIDProvider, Signer, Verifier, Encryptor, Decryptor interfaces
- **Deps**: stdlib only
- **Imported by**: pkg/acp, pkg/channel, pkg/delegation, pkg/sdk
- **Status**: ✅ Clean, minimal

### 📦 `pkg/config` — Configuration
- **Files**: `config.go`, `config_test.go`
- **Deps**: stdlib only
- **Initialized**: `cmd/studio/main.go:395` — `DefaultConfig()` + `ValidateConfig()`
- **Status**: ✅

### 📦 `pkg/crypto` — Cryptography Core (7 files)
- **Files**: `identity.go`, `identity_test.go`, `identity_limiter.go`, `identity_limiter_test.go`, `keystore.go`, `keystore_test.go`, `mnemonic.go`, `pow.go`, `pow_test.go`, `recovery.go`, `recovery_test.go`, `sign.go`, `sign_test.go`
- **Deps**: stdlib only
- **🔗 Imported by**: 15 packages (cmd/agent, cmd/founder, cmd/gateway, cmd/seed, cmd/studio, pkg/acp, pkg/channel, pkg/delegation, pkg/identity, pkg/naming, pkg/node, pkg/node/subsystems, pkg/sdk, pkg/search, pkg/session/connection)
- **Status**: ⚠️
- **🐛 `recovery.go:5`**: 🔴 Import `hashicorp/vault/shamir` non-existent → **compile error**
- **🐛 `identity_limiter.go:37`**: Uses `pkg.Identifier` type that doesn't exist OR duplicates `pkg/identity/limiter.go`
- **✅ `sign.go`**: Exports `SignPayloadHex`, `VerifyPayloadHex`, `DomainDomainFounder`, `DomainDomainOwner`, `DomainSearch` — all verified working

### 📦 `pkg/eventbus` — Event Bus
- **Files**: `bus.go`, `bus_test.go`
- **Deps**: stdlib only
- **🔗 Imported by**: 10 packages (api, cmd/studio, analytics, backup, ceo, notifications, orchestrator, plugins, session, upgrade)
- **Status**: ⚠️
- **🐛 `bus.go:132-145`**: TOCTOU race — Publish() checks `running` (line 134) under RLock, releases lock (line 135), THEN tries `eventQueue <- event` (line 145). Stop() could close channel between check and send. `default` case at line 147 MAY prevent panic (needs Go runtime verification). Double close is properly protected (line 175-177). Low urgency — only triggers during shutdown race.

### 📦 `pkg/protocol` — Protocol Messages
- **Files**: `messages.go`
- **Deps**: stdlib only
- **🔗 Imported by**: pkg/acp, pkg/channel, pkg/content, pkg/gateway, pkg/node, pkg/search
- **Status**: ✅

### 📦 `pkg/storage` — Storage (Erasure + Quota)
- **Files**: `erasure.go`, `erasure_test.go`, `quota.go`, `quota_test.go`
- **Deps**: `klauspost/reedsolomon` (external)
- **🔗 Imported by**: cmd/studio, pkg/content, pkg/node, pkg/orchestrator
- **Status**: ⚠️
- **🐛**: No GC goroutine for Badger → disk bloat over time

### 📦 `pkg/policy` — Policy Engine
- **Files**: `approvals.go`, `approvals_test.go`, `engine.go`, `engine_test.go`, `types.go`
- **Deps**: stdlib only
- **🔗 Imported by**: cmd/studio, pkg/agent_bridge, pkg/capability, pkg/capability/github, pkg/capability/gmail, pkg/capability/messaging, pkg/capability/pipeline, pkg/runtime, pkg/workflow
- **Status**: ⚠️
- **🐛**: Uses `map[string]interface{}` — no type safety
- **🐛**: Policy audit log writes to stdout, not file

### 📦 `pkg/security` — TLS + Rate Limit
- **Files**: `ratelimit.go`, `ratelimit_test.go`, `tls.go`, `tls_test.go`
- **Deps**: stdlib + `golang.org/x/time/rate`
- **🔗 Imported by**: api
- **Status**: ⚠️
- **🐛**: Self-signed TLS with no cert pinning

### 📦 `pkg/security/core` — Security Tokens
- **Files**: `security.go`
- **Deps**: stdlib + zap
- **Status**: ⚠️
- **🐛**: Security token generation with no rate limit

### 📦 `pkg/validation` — Input Validation
- **Files**: `validator.go`, `validator_test.go`
- **Deps**: stdlib only
- **Initialized**: `cmd/studio/main.go:413-418`
- **Status**: ✅

### 📦 `pkg/limits` — Rate/Concurrency Limits
- **Files**: `limits.go`, `limits_test.go`
- **Deps**: stdlib only
- **Initialized**: `cmd/studio/main.go:402-406`
- **Status**: ⚠️
- **🐛**: Token bucket not reset on config change

### 📦 `pkg/timeout` — Timeout Utilities
- **Files**: `timeout.go`, `timeout_test.go`
- **Deps**: stdlib only
- **Initialized**: `cmd/studio/main.go:409`
- **Status**: ✅

### 📦 `pkg/logger` — Logger
- **Files**: `logger.go`, `logger_test.go`
- **Deps**: stdlib + zap
- **Initialized**: `cmd/studio/main.go:382-391`
- **Status**: ✅

### 📦 `pkg/network` — Network Transport
- **Files**: `bootstrap.go`, `bootstrap_test.go`
- **Deps**: stdlib only
- **Status**: ✅
- **Note**: Noise handshake IS configured at libp2p host level (node.go:99). `connectToPeer()` applies 30s timeout (bootstrap.go:153). My earlier claims were wrong.

### 📦 `pkg/network/domain` — DNS + HTTP Proxy
- **Files**: `http_proxy.go`, `local_dns_proxy.go`, `p2p_dns_resolver.go`, `system_proxy.go`
- **Deps**: stdlib only
- **🔗 Imported by**: cmd/studio
- **Initialized**: `cmd/studio/main.go:521-541`
- **Status**: ✅

### 📦 `pkg/registry` — Agent/Service Registry
- **Files**: `manifest.go`, `registry.go`, `registry_test.go`
- **Deps**: stdlib only
- **🔗 Imported by**: pkg/discovery, pkg/sdk
- **Status**: ⚠️
- **🐛**: No TTL → stale entries accumulate

### 📦 `pkg/sandbox` — WASM Sandbox
- **Files**: `executor.go`, `executor_test.go`
- **Deps**: `tetratelabs/wazero` (external)
- **Initialized**: `cmd/studio/main.go:437`
- **Status**: ⚠️
- **🐛**: `os/exec` with user-supplied name → command injection risk

### 📦 `pkg/providers` — LLM Provider System
- **Files**: `api_key_manager.go`, `free_models_tracker.go`, `free_router.go`, `model_catalog.go`, `register.go`, `router.go`, `types.go`
- **Deps**: stdlib only
- **🔗 Imported by**: cmd, cmd/studio, pkg/agent/integration, pkg/agent/unified, pkg/providers/builtin + 20 builtin providers
- **Status**: ⚠️
- **Note**: `free_router.go:155-156` — `defer cancel()` inside loop accumulates defers but all execute on function return. No context leak (my earlier claim was wrong).
- **⚠️ `model_catalog.go:271`**: `contains()` = `strings.Contains` → false positives in search
- **⚠️ `model_catalog.go:310`**: `SortModels` sorts by capacity but capacity is never set

### 📦 `pkg/providers/builtin` — LLM Provider Implementations
- **Files**: `register.go`, `test_connection.go` + 20 provider dirs (anthropic, cohere, custom, deepseek, google, groq, mistral, moonshot, nvidia, ollama, openai, openrouter, perplexity, poolside, qwen, recraft, sourceful, stepfun, tencent, togetherai, xai, xiaomi, zai)
- **🔗 Imported by**: cmd, cmd/studio
- **Initialized**: `cmd/studio/main.go:331`
- **Status**: ⚠️ — Most providers are wrappers, individual provider quality varies

### 📦 `pkg/ledger` — Cost Tracking
- **Files**: `cost_tracker.go`, `cost_tracker_test.go`, `credit_manager.go`, `credit_manager_test.go`
- **Deps**: stdlib only
- **Initialized**: `cmd/studio/main.go:421-423`
- **Status**: ✅

### 📦 `pkg/analytics/core` — Analytics Engine
- **Files**: `analytics.go`
- **Deps**: stdlib only
- **🔗 Imported by**: pkg/analytics
- **Status**: ✅

### 📦 `pkg/backup/core` — Backup Engine
- **Files**: `backup.go`
- **Deps**: stdlib only
- **Status**: ⚠️ — No compression

### 📦 `pkg/upgrade/core` — Upgrade Engine
- **Files**: `upgrade.go`
- **Deps**: stdlib + zap
- **Status**: ⚠️ — No version comparison logic

### 📦 `pkg/notifications/core` — Notification Engine
- **Files**: `notifications.go`
- **Deps**: stdlib only
- **Status**: ✅

### 📦 `pkg/capability` — Capability System
- **Files**: `types.go`, `manager.go`, `manager_test.go`, `capability_test.go`
- **Deps**: pkg/policy
- **🔗 Imported by**: pkg/capability/github, gmail, messaging, pipeline, pkg/orchestrator, pkg/runtime, pkg/workflow
- **Status**: ⚠️
- **🐛**: `Capability.Encode()` returns nil — not implemented

### 📦 `pkg/acp` — ACP Protocol
- **Files**: `handler.go`, `message.go`, `message_test.go`, `tasks.go`, `tasks_test.go`, `transport.go`
- **Deps**: pkg/common, pkg/crypto, pkg/protocol
- **🔗 Imported by**: cmd/studio, pkg/node, pkg/node/subsystems
- **Initialized**: `cmd/studio/main.go:562` — `acp.NewRouter()`
- **Status**: ⚠️
- **🐛**: ACP token exchanged but never validated after receipt

---

## LAYER 1 — Depend on Layer 0

### 📦 `pkg/identity` — Identity Management
- **Files**: `delegation.go`, `limiter.go`, `manager.go`, `manager_test.go`, `persistence.go`, `persistence_test.go`, `record.go`, `record_test.go`, `revocation.go`, `revocation_test.go`
- **Deps**: pkg/crypto
- **🔗 Imported by**: cmd/founder, cmd/gateway, cmd/seed, cmd/studio, pkg/node, pkg/node/subsystems
- **Initialized**: `cmd/studio/main.go:92`
- **Status**: ✅

### 📦 `pkg/naming` — Domain Naming System
- **Files**: `commit.go`, `commit_test.go`, `domain.go`, `domain_test.go`, `ns.go`
- **Deps**: pkg/crypto + `golang.org/x/text`
- **🔗 Imported by**: api, cmd/founder, pkg/node
- **Status**: ✅
- **Note**: `hasMixedScript()` intentionally enforces ASCII-only (homograph protection, comment at ns.go:60). This is a security measure, not a bug.

### 📦 `pkg/channel` — Communication Channels
- **Files**: `private.go`, `private_test.go`, `pubsub.go`, `rotation.go`, `rotation_test.go`, `threaded.go`, `threaded_test.go`
- **Deps**: pkg/common, pkg/crypto, pkg/protocol
- **🔗 Imported by**: pkg/node, pkg/orchestrator
- **Status**: ✅
- **Note**: No `sync.Once` in pubsub.go (my earlier claim was wrong). Topic management uses simple map lookup in node.go's `JoinChannel()`. Rejoin is fully possible.

### 📦 `pkg/content` — Content/Storage (IPFS-like)
- **Files**: `provider.go`, `retrieval.go`, `store.go`
- **Deps**: pkg/protocol, pkg/storage
- **🔗 Imported by**: pkg/mailbox, pkg/node, pkg/node/subsystems, pkg/orchestrator, pkg/workflow
- **Status**: ⚠️
- **⚠️ `provider.go:53-64`**: `AddProvider()` creates `ProviderRecord` with only current node (`providers = []string{host.ID()}`). `PutValue` overwrites existing records. Multiple nodes can't coexist as providers for same CID. Bitswap serving (lines 89-120) works independently and is fine.
- **✅ API verified**: `BlockStore` interface, `CIDFromData()`, `NewBadgerBlockStore()`, `NewMemoryBlockStore()` all exist

### 📦 `pkg/search` — Distributed Search Index
- **Files**: `index.go`
- **Deps**: pkg/crypto, pkg/protocol
- **🔗 Imported by**: pkg/node, pkg/node/subsystems
- **Status**: ✅
- **Exports**: `IndexEntry`, `NewIndexEntry`, `TokenBucket`, `NewTokenBucket` — all verified

### 📦 `pkg/delegation` — Delegation System
- **Files**: `advanced.go`, `advanced_test.go`, `integration.go`
- **Deps**: pkg/common, pkg/crypto
- **Initialized**: `cmd/studio/main.go:481-485`
- **Status**: ✅

### 📦 `pkg/gateway` — API Gateway
- **Files**: `manifest.go`, `manifest_test.go`, `server.go`
- **Deps**: pkg/node, pkg/protocol
- **🔗 Imported by**: cmd/gateway
- **Status**: ⚠️
- **🐛**: Gateway has no TLS support

---

## LAYER 2 — Depend on Layer 0+1

### 📦 `pkg/node` — Core Node (Orchestrator Hub)
- **Files**: `acp.go`, `channel_ops.go`, `config.go`, `dht_prefix_test.go`, `direct.go`, `direct_test.go`, `domain_ops.go`, `integration_test.go`, `node.go`, `validator.go`
- **Deps**: pkg/acp, pkg/channel, pkg/content, pkg/crypto, pkg/identity, pkg/naming, pkg/network, pkg/node/subsystems, pkg/protocol, pkg/search, pkg/storage
- **🔗 Imported by**: api, cmd/founder, cmd/gateway, cmd/seed, cmd/studio, pkg/agent_bridge, pkg/gateway
- **Initialized**: `cmd/studio/main.go:106`
- **Status**: 🔴
- **🐛 `node.go:173`**: `founderPub` hardcoded to `nil` → all domain scope queries fail

### 📦 `pkg/node/subsystems` — Node Subsystems
- **Files**: `identity.go`, `messaging.go`, `network.go`, `security.go`, `storage.go`, `subsystems_test.go`
- **Deps**: pkg/acp, pkg/content, pkg/crypto, pkg/identity, pkg/search
- **🔗 Imported by**: pkg/node, pkg/sdk
- **Status**: ✅

### 📦 `pkg/mailbox` — Secure Message Mailbox
- **Files**: `mailbox.go`, `mailbox_test.go`
- **Deps**: pkg/content
- **🔗 Imported by**: pkg/integration, pkg/orchestrator
- **Status**: ✅
- **Note**: I previously claimed this was broken (imagined goroutines + channels). The actual code is purely synchronous with proper error handling. `Send()` encrypts and stores via `BlockStore.Put()`. `Fetch()` iterates keys with `continue` on error (no panic). Both functions work correctly.

### 📦 `pkg/vault` — Secret Vault
- **Files**: `time.go`, `vault.go`, `vault_test.go`
- **Deps**: pkg/vault/encryption, pkg/vault/keyprovider + scrypt
- **Status**: ✅

### 📦 `pkg/vault/encryption` — AES-GCM Encryption
- **Files**: `encryption.go`, `encryption_test.go`
- **Deps**: stdlib only
- **Status**: ✅

### 📦 `pkg/vault/keyprovider` — Key File Provider
- **Files**: `file.go`, `file_test.go`, `keyprovider.go`
- **Deps**: stdlib only
- **Status**: ✅

### 📦 `pkg/session` — Session Management (14 files)
- **Files**: `aggregator.go`, `chat.go`, `chat_test.go`, `container.go`, `final_reviewer.go`, `handoff_manager.go`, `memory.go`, `placeholders.go`, `progress_tracker.go`, `retry.go`, `session_bridge.go`, `session_bridge_manager.go`, `session_bridge_test.go`, `skills.go`, `task_manager.go`, `task_manager_test.go`, `workflow.go`
- **Deps**: pkg/agent, pkg/eventbus + badger, uuid
- **🔗 Imported by**: api, cmd/studio, pkg/agent/integration, pkg/agent/unified, pkg/orchestrator
- **Initialized**: `cmd/studio/main.go:168` — `SessionBridgeManager`
- **Status**: ⚠️
- **Note**: Coexists with `pkg/session/core` — 2 session systems active

### 📦 `pkg/session/core` — Unified Session Manager
- **Files**: `manager.go`
- **Deps**: stdlib + zap only (clean!)
- **🔗 Imported by**: api, cmd/studio, pkg/integration, scripts
- **Initialized**: `cmd/studio/main.go:164`
- **Status**: ✅ Clean design

### 📦 `pkg/session/advanced` — Advanced Session Manager
- **Files**: `advanced_manager.go`
- **Deps**: stdlib + zap only
- **🔗 Imported by**: (none) — **ISOLATED**
- **Status**: ⚠️ — Not referenced anywhere

### 📦 `pkg/session/connection` — TCP/Connection Session
- **Files**: `connection.go`, `session.go`
- **Deps**: stdlib + logrus
- **🔗 Imported by**: (none) — **ISOLATED**
- **Status**: ⚠️ — uses sirupsen/logrus directly (inconsistent with rest of project)

### 📦 `pkg/agent` — Agent System (4 files)
- **Files**: `adapter.go`, `adapter_test.go`, `instance_tracker.go`, `registry.go`, `registry_human_client_test.go`, `registry_test.go`, `reservation_manager.go`, `reservation_manager_test.go`
- **Deps**: stdlib only
- **🔗 Imported by**: cmd/studio, examples, pkg/agent/adapters, pkg/ceo, pkg/integration, pkg/orchestrator, pkg/session, scripts
- **Initialized**: `cmd/studio/main.go:152`
- **Status**: ✅

### 📦 `pkg/agent/adapters` — Agent Adapters (13 files)
- **Files**: `api_adapter.go`, `browser_adapter.go`, `cli_adapter.go`, `custom_adapter.go`, `desktop_adapter.go`, `hook_system.go`, `ide_adapter.go`, `ide_extension_adapter.go`, `instance_manager.go`, `local_adapter.go`, `multi_cli_adapter.go`, `multi_desktop_adapter.go`, `multi_ide_adapter.go`
- **Deps**: pkg/agent
- **🔗 Imported by**: cmd/studio, examples, pkg/integration
- **Initialized**: `cmd/studio/main.go:251-281`
- **Status**: ✅

### 📦 `pkg/agent/unified` — Unified Agent (22 files — largest)
- **Files**: `agent_executor.go`, `coordinator.go`, `data_curator.go`, `error_handler.go`, `file_watcher.go`, `flow_manager.go`, `local_memory_cache.go`, `memory_integration.go`, `platform_sync.go`, `problem_solution_registry.go`, `process_monitor.go`, `realtime_memory_sync.go`, `realtime_skill_sync.go`, `session_event_bus.go`, `session_manager.go`, `session_manager_test.go`, `skill_integration.go`, `task_scheduler.go`, `unified_agent.go`, `unified_agent_sync.go`, `unified_agent_test.go`, `unified_memory_manager.go`, `unified_skill_manager.go`, `unified_sync_manager.go`
- **Deps**: pkg/agent/automation, direction, integration, skills, subagents, tools, validation, pkg/providers, pkg/session
- **🔗 Imported by**: cmd/agent, cmd/studio
- **Initialized**: `cmd/studio/main.go:301`
- **Status**: ⚠️ — Central orchestration hub, 0 test files for critical paths

---

## LAYER 3 — Depend on Layer 2

### 📦 `pkg/agent_bridge` — Agent Bridge Protocol
- **Files**: `client.go`, `client_test.go`, `middleware.go`, `middleware_test.go`, `multiplexed_bridge.go`, `multiplexed_bridge_test.go`, `server.go`, `server_test.go`, `session_manager.go`, `session_manager_test.go`, `task_protocol.go`, `task_protocol_test.go`, `tools.go`, `tools_test.go`
- **Deps**: pkg/agent_bridge/protocol, pkg/node, pkg/policy
- **🔗 Imported by**: cmd/agent, pkg/orchestrator
- **Status**: ✅ — Well tested (7 test files)

### 📦 `pkg/agent_bridge/protocol` — Bridge Protocol Types
- **Files**: `protocol.go`
- **Deps**: stdlib only
- **Status**: ✅

### 📦 `pkg/email` — P2P Email System
- **Files**: `email.go`, `email_store.go`, `email_test.go`, `email_types.go`, `integration.go`, `p2p_email_service.go`
- **Deps**: pkg/orchestrator
- **Initialized**: `cmd/studio/main.go:240-245` + `cmd/studio/main.go:512-518`
- **Status**: ✅

### 📦 `pkg/hosting` — P2P Hosting System
- **Files**: `hosting.go`, `hosting_test.go`, `hosting_types.go`, `integration.go`, `p2p_hosting_service.go`, `site_uploader.go`
- **Deps**: pkg/orchestrator
- **Initialized**: `cmd/studio/main.go:463-464` + `cmd/studio/main.go:544-546`
- **Status**: ✅

### 📦 `pkg/orchestrator` — Agent Orchestrator (17 files)
- **Files**: `a2a_protocol.go`, `a2a_protocol_test.go`, `agent_lifecycle.go`, `aggregator.go`, `chat_connector.go`, `chat_connector_test.go`, `comprehensive_logger.go`, `comprehensive_logger_test.go`, `connector.go`, `connector_connect_test.go`, `connector_human_client_test.go`, `connector_test.go`, `delegation_manager.go`, `email_mailbox_integration_test.go`, `email_system.go`, `email_system_test.go`, `external_platforms.go`, `external_platforms_test.go`, `failure_handler.go`, `final_reviewer.go`, `mcp_protocol.go`, `mcp_protocol_test.go`, `orchestrator_engine.go`, `role_assigner.go`, `session_event_broadcaster.go`, `session_event_broadcaster_test.go`, `session_manager.go`, `storage_connector.go`, `storage_connector_test.go`
- **Deps**: pkg/agent, pkg/agent_bridge, pkg/agent_bridge/protocol, pkg/capability, pkg/channel, pkg/content, pkg/eventbus, pkg/mailbox, pkg/session, pkg/storage, pkg/verification
- **🔗 Imported by**: cmd/studio, pkg/email, pkg/hosting
- **Initialized**: (via emailManager)
- **Status**: 🔴
- **🐛**: Uses `pkg/mailbox` which is BROKEN → any mailbox operation crashes
- **🐛 `session_manager.go`**: 🔴 **DEAD CODE** — `NewSessionManager()` is **never called** anywhere in the codebase (grep returns 0 results). This is a 4th session management system that nobody initialized.
- **Note**: Has 10+ test files — most tested package

### 📦 `pkg/integration` — Integration Layer (7 files)
- **Files**: `agent_communication.go`, `agent_session_integration.go`, `instance_session_integration.go`, `integration_test.go`, `role_assignment.go`, `session_orchestrator.go`, `task_routing.go`, `webhook_router.go`, `webhook_router_test.go`
- **Deps**: pkg/agent, pkg/agent/adapters, pkg/mailbox, pkg/session/core
- **🔗 Imported by**: (none directly — used implicitly)
- **Status**: 🔴
- **🐛 `webhook_router.go:34`**: Calls `mb.Send()` from BROKEN mailbox → webhooks crash
- **🐛 `role_assignment.go:60`**: `AssignRole()` doesn't store role anywhere → useless
- **🐛 `agent_session_integration.go:140`**: `delete(session.AgentInstances, instanceKey)` — deletes from wrong key

### 📦 `pkg/sdk` — Client SDK
- **Files**: `client.go`, `client_test.go`, `crdt_sync.go`, `crdt_sync_test.go`, `presence.go`, `presence_test.go`
- **Deps**: pkg/common, pkg/crypto, pkg/node/subsystems, pkg/registry
- **Status**: ✅ (has tests)

### 📦 `pkg/capability/github` — GitHub API Capability
- **Files**: `github.go`, `github_test.go`
- **Deps**: pkg/capability, pkg/policy
- **Status**: ✅ (has tests)

### 📦 `pkg/capability/gmail` — Gmail API Capability
- **Files**: `gmail.go`, `gmail_test.go`
- **Deps**: pkg/capability, pkg/policy
- **Status**: ✅ (has tests)

### 📦 `pkg/capability/messaging` — Messaging Capability
- **Files**: `messaging.go`, `messaging_test.go`
- **Deps**: pkg/capability, pkg/policy
- **Status**: ✅ (has tests)

### 📦 `pkg/capability/pipeline` — Pipeline Capability
- **Files**: `pipeline.go`, `pipeline_test.go`
- **Deps**: pkg/capability, pkg/policy, pkg/runtime/observability
- **Status**: ✅ (has tests)

### 📦 `pkg/runtime` — Agent Runtime
- **Files**: `context.go`, `runtime.go`, `runtime_test.go`
- **Deps**: pkg/capability, pkg/capability/pipeline, pkg/policy, pkg/runtime/events, knowledge, lifecycle, observability, scheduler, state
- **Status**: ✅

### 📦 `pkg/runtime/events` — Runtime Event Bus
- **Files**: `bus.go`, `bus_test.go`, `event.go`
- **Deps**: stdlib only
- **Note**: Third event system (besides pkg/eventbus and pkg/events/*)
- **Status**: ✅

### 📦 `pkg/runtime/knowledge` — Knowledge Store
- **Files**: `store.go`, `store_test.go`
- **Deps**: stdlib only
- **Status**: ✅

### 📦 `pkg/runtime/scheduler` — Task Scheduler
- **Files**: `scheduler.go`, `scheduler_test.go`
- **Deps**: pkg/runtime/events
- **Status**: ✅

### 📦 `pkg/runtime/observability` — Observability
- **Files**: `audit.go`, `logger.go`, `metrics.go`, `observability_test.go`, `tracer.go`
- **Deps**: stdlib only
- **Status**: ✅

### 📦 `pkg/runtime/state` — State Store
- **Files**: `store.go`, `store_test.go`
- **Deps**: stdlib only
- **Status**: ✅

### 📦 `pkg/runtime/lifecycle` — Lifecycle Management
- **Files**: `lifecycle.go`, `lifecycle_test.go`
- **Deps**: stdlib only
- **Status**: ✅

### 📦 `pkg/runtime/sandbox` — Runtime Sandbox
- **Files**: `sandbox.go`, `sandbox_test.go`
- **Deps**: stdlib only
- **Status**: ✅

---

## ISOLATED / UNREFERENCED PACKAGES

### 📦 `pkg/session/advanced` — Advanced Session Manager
- **Imported by**: (none)
- **Status**: ⚠️ — Code exists but no one imports it

### 📦 `pkg/session/connection` — Connection-based Sessions
- **Imported by**: (none)
- **Status**: ⚠️ — Uses logrus (inconsistent), no callers

### 📦 `pkg/memory/*` — Memory System (6 files)
- `cache/local_cache.go`, `core/memory.go`, `integration/integration.go`, `storage/memory_storage.go`, `sync/realtime_sync.go`, `types/entry.go`
- **Imported by**: (none)
- **Status**: ⚠️ — Isolated, no callers in codebase

### 📦 `pkg/events/*` — Events System (5 files)
- `broadcast/broadcaster.go`, `core/bus.go`, `runtime/runtime_events.go`, `session/session_bus.go`, `types/event_types.go`
- **Imported by**: (none)
- **Status**: ⚠️ — Third event system, entirely unused

### 📦 `pkg/agent/automation` — Automation Manager
- **Imported by**: only by pkg/agent/unified
- **Status**: ✅ — Used internally by unified agent

### 📦 `pkg/agent/collaboration` — Agent Collaboration
- **Imported by**: only by pkg/agent/integration
- **Status**: ✅ — Used internally

### 📦 `pkg/agent/learning` — Learning Engine
- **Imported by**: only by pkg/agent/integration
- **Status**: ✅ — Used internally

### 📦 `pkg/agent/memory` — Agent Memory
- **Imported by**: only by pkg/agent/integration
- **Status**: ✅ — Used internally

### 📦 `pkg/agent/quality` — Quality Checker
- **Imported by**: only by pkg/agent/integration
- **Status**: ✅ — Used internally

### 📦 `pkg/agent/skills` — Skills System
- **Imported by**: pkg/agent/direction, pkg/agent/unified
- **Status**: ✅

### 📦 `pkg/agent/subagents` — Subagent Manager
- **Imported by**: pkg/agent/unified
- **Status**: ✅

### 📦 `pkg/agent/tasks` — Task Decomposer
- **Imported by**: pkg/agent/integration
- **Status**: ✅

### 📦 `pkg/agent/thinking` — Thinking Engine
- **Imported by**: pkg/agent/integration
- **Status**: ✅

### 📦 `pkg/agent/tools` — Tools Executor
- **Files**: `executor.go`, `file_lock.go`
- **Imported by**: pkg/agent/unified
- **Status**: ✅

### 📦 `pkg/agent/tracking` — Activity Tracker
- **Imported by**: pkg/agent/integration
- **Status**: ✅

### 📦 `pkg/agent/validation` — Multi-layer Validator
- **Imported by**: pkg/agent/unified
- **Status**: ✅

---

## ENTRY POINTS (cmd/)

### 📦 `cmd/studio/main.go` — Main Application (710 lines)
- **Imports 34 packages** — all initialized
- **Flow**:
  1. Parse flags → Generate keypair → Create identity → Create node
  2. Publish identity → Create QuotaManager → EventBus → BadgerDB
  3. AgentRegistry → ReservationManager → UnifiedSessionManager → SessionBridgeManager
  4. Create 3 sessions + bridges → EmailManager
  5. Register adapters (CLI, IDE, Browser, Custom)
  6. SessionContainer → UnifiedAgent → ProviderRegistry → Router
  7. Execute test task → CEO Supervisor
  8. Initialize all isolated packages (limits, timeout, validation, ledger, etc.)
  9. Analytics → Backup → Delegation → Notifications → Plugins → Upgrade
  10. P2P Email → P2P DNS → HTTP Proxy → System Proxy → P2P Hosting
  11. Verification (5 verifiers) → ACP Router → Policy Engine (6 rules)
  12. API Server → WebSocket Bridge → Wait for signal → Shutdown

### 📦 `cmd/main.go` — Alternative entry
- **Deps**: pkg/providers, pkg/providers/builtin
- **Status**: ⚠️ — Minimal, likely test entry

### 📦 `cmd/agent/main.go` — Agent standalone
- **Deps**: pkg/agent/unified, pkg/agent_bridge, pkg/crypto
- **Status**: ✅

### 📦 `cmd/founder/main.go` — Founder node
- **Deps**: pkg/crypto, pkg/identity, pkg/naming, pkg/node
- **Status**: ✅

### 📦 `cmd/gateway/main.go` — Gateway node
- **Deps**: pkg/crypto, pkg/gateway, pkg/identity, pkg/node
- **Status**: ✅

### 📦 `cmd/seed/main.go` — Seed node
- **Deps**: pkg/crypto, pkg/identity, pkg/node
- **Status**: ✅

---

## API LAYER

### 📦 `api/` — REST + WebSocket API
- **Files**: `dashboard.go`, `local_ws_bridge.go`, `rest.go`, `rest_test.go`
- **Deps**: pkg/eventbus, pkg/naming, pkg/node, pkg/protocol, pkg/security, pkg/session, pkg/session/core
- **Initialized**: `cmd/studio/main.go:658-670`
- **Status**: 🔴
- **🐛 CRITICAL: `rest.go:122-128`**: **API creates its OWN EventBus + SessionManager** — `NewServerWithTLS()` creates `sessioncore.NewUnifiedSessionManager()` (line 122), `eventbus.NewEventBus()` (line 125), and `session.NewSessionBridgeManager()` (line 128), ALL separate from the ones in `cmd/studio/main.go`. This means REST API runs on a **completely isolated EventBus** — events from main.go invisible to API and vice versa, WebSocket bridge (EventBus #1) disconnected from REST API (EventBus #2), 2 duplicate SessionManagers running independently.
- **🐛 `rest.go:182-192`**: `getSlice()` type assertion fails for `[]string` → returns nil silently
- **🐛 `rest.go:211-222`**: `getInt()` truncates floats → loses precision
- **🐛**: Dashboard and WS bridge not fully tested

---

## SCRIPTS & EXAMPLES

### 📦 `scripts/`
- `test_multi_instance.go` — Test multi-instance (deps: pkg/agent, pkg/session/core)
- `providers/test_all_providers.go` — Test all LLM providers (deps: stdlib only)

### 📦 `examples/`
- `multi_agent_example.go` — Example multi-agent (deps: pkg/agent, pkg/agent/adapters)

---

## 🚨 REAL BUGS (Verified by Code Review + Independent Analysis)

| Priority | File | Line | Bug | Status |
|----------|------|------|-----|--------|
| 🔴 P0 | `api/rest.go` | 1617-1636 | `getInt()` asserts `.(int)` but JSON numbers are `float64`; `getSlice()` asserts `.([]string)` but JSON arrays are `[]interface{}`. All numeric fields = 0, all slice fields = empty | **CONFIRMED** |
| 🔴 P0 | `pkg/node/node.go` + `pkg/node/subsystems/security.go` | 173 + 15-19 | `founderPub` parsed (line 113) but NEVER passed to `SecuritySubsystem` (missing param at line 173). `n.founderPub()` returns nil → domain signature verification fails | **CONFIRMED** |
| 🔴 P0 | `pkg/integration/role_assignment.go` | 47-70 | `AssignRole()` validates capabilities + logs, but **stores nothing**. No map, no DB write. `GetAgentsByRole()` returns all agents (can't filter). Comments at line 231-233 acknowledge this is unfinished | **CONFIRMED** |
| 🔴 P0 | `pkg/integration/agent_session_integration.go` | 140-141 | `delete(session.AgentInstances, instanceKey)` uses composite key `agentID-instanceID`, but map is keyed by simple `agentID`. Delete is a silent no-op | **CONFIRMED** |
| 🟠 P1 | `pkg/eventbus/bus.go` | 132-145 | TOCTOU race: Publish() checks `running` under RLock (134), releases (135), then sends to `eventQueue` (145). Stop() could close channel between check and send. `default` case at 147 may protect — needs Go runtime verification | **CONFIRMED** |
| 🟠 P1 | `pkg/providers/model_catalog.go` | 271 | `contains()` = `strings.Contains` → false positives | **CONFIRMED** |
| 🟠 P1 | `pkg/providers/model_catalog.go` | 310 | SortModels sorts by unset capacity field | **CONFIRMED** |
| 🟠 P1 | `pkg/content/provider.go` | 53-64 | AddProvider creates record with only self, PutValue overwrites others | **CONFIRMED** |
| 🟡 P2 | `pkg/integration/webhook_router.go` | 34 | Webhook router — integration package is currently isolated, so no immediate impact | **LOW URGENCY** |

### ❌ CLAIMS I MADE THAT WERE WRONG:
- **`pkg/mailbox`**: Code is fine. Send() is synchronous (no goroutines). Fetch() uses `continue` on errors (no panic). **NOT BROKEN**
- **`pkg/crypto/recovery.go`**: Import `hashicorp/vault/shamir` IS valid — subpackage of vault v1.21.4 module. **COMPILES FINE**
- **`pkg/providers/free_router.go`**: `defer cancel()` in loop — defers accumulate but all execute on return. **NO LEAK**
- **`pkg/naming/ns.go`**: Intentional homograph protection. **BY DESIGN**
- **`pkg/channel/pubsub.go`**: No `sync.Once` exists. **MY MISTAKE**
- **`pkg/network/bootstrap.go`**: Noise handshake at host level (node.go:99). 30s timeout (bootstrap.go:153). **CORRECT AS-IS**
- **"API creates duplicate EventBus"**: API is a modular component; its own EventBus is by design. WS bridge uses main.go's EventBus. **ARCHITECTURAL CHOICE** (not a bug per se, but worth noting for future integration)
- **"5 session managers"**: `pkg/agent_bridge/session_manager.go` is a TCP connection tracker (different purpose). The rest serve different roles. **NOT DUPLICATION**

---

## KEY ARCHITECTURAL PATTERNS

### Session-Centric Architecture Vision
```
[Human Client UI/IDE] ←→ WebSocket (local_ws_bridge.go) ←→ EventBus (#1)
                                 ↕
                          SessionContainer (THE CORE)
                          ├── CollectiveMemory (4 types: Episodic/Semantic/Procedural/Meta)
                          ├── SkillsManager (shared skills across team)
                          ├── WorkflowEngine (16-phase project management)
                          ├── TaskManager (task distribution)
                          ├── ProgressTracker (real-time % tracking)
                          ├── HandoffManager (artifact delivery agent→agent)
                          ├── Aggregator (project assembly from artifacts)
                          ├── FinalReviewer (quality gate)
                          └── ChatManager (real-time messaging)
                                 ↕
                          EventBus (#1) — Central Nervous System
                          ↕           ↕            ↕
                   pkg/orchestrator  pkg/agent    REST API (EventBus #2, separate)
                   (lifecycle,       (local       (for external CRUD operations)
                    role assign,      agents
                    delegation)       via TCP
                                     bridge)
```

### Init Flow (cmd/studio/main.go)
```
crypto.GenerateKeyPair()
  → identity.NewIdentityRecord()
    → node.New() [libp2p hub]
      → storage.NewQuotaManager()
        → eventbus.NewEventBus() ← EventBus #1 (shared across system)
          → badger.Open()
            → agent.NewAgentRegistry()
              → session/core.NewUnifiedSessionManager()
                → session.NewSessionBridgeManager() [using EventBus #1]
                  → session.NewSessionContainer() [using EventBus #1]
                    → adapters (CLI, IDE, Browser, Custom)
                      → unified.NewUnifiedAgent()
                        → providers.NewRouter()
                          → ceo.NewCEOSupervisor()
                            → all isolated packages (in integration)
                              → api.NewServer() ← creates its own EventBus #2 (modular component)
                                → wsHandler.Start() ← uses EventBus #1 (real-time client updates)
```

### Development Phases (Built by Devon)
- **Phase 1**: Core infrastructure (crypto, node, identity, network, content, DHT)
- **Phase 2**: Session system + agent management + orchestrator
- **Phase 3**: Cursor-like features (skills, subagents, automation, MCP integration)
- **Phase 4**: API + WebSocket bridge + dashboard (being integrated)
- **Phase 5**: Isolated packages being wired in (analytics, backup, notifications, etc.)

### Session Manager Files — Their TRUE Roles
```
File                                    │ Role                         │ Status
────────────────────────────────────────┼──────────────────────────────┼─────────────
pkg/session/core/manager.go             │ Session CRUD + client mgmt   │ ✅ Clean design
pkg/session/session_bridge_manager.go   │ Bridge sessions together      │ ✅ Clean design  
pkg/agent/unified/session_manager.go    │ AgentExecutor pattern         │ ✅ Refactored version (has AgentExecutor interface)
pkg/agent_bridge/session_manager.go     │ TCP connection tracker        │ ✅ For bridge protocol (NOT session management)
pkg/orchestrator/session_manager.go     │ Dead code (never called)      │ 🔴 DEFUNCT (NewSessionManager never instantiated)
```
- These are NOT 5 competing systems. Each has a distinct purpose except `pkg/orchestrator/session_manager.go` which is truly dead code.
- `docs/SESSION_MANAGER_CONFLICT_ANALYSIS.md` documents the historical conflict and the AgentExecutor refactoring fix (partially implemented in `pkg/agent/unified/session_manager.go:38`).

### Event Systems — Their TRUE Roles
```
pkg/eventbus       — Central event bus for ALL components (10 importers)
pkg/events/*       — Different purpose (isolated event definitions, NOT competing)
pkg/runtime/events — Internal to pkg/runtime package
```
- These are NOT competing. `pkg/events/*` was an older experimental module. `pkg/runtime/events` is internal to the runtime package.

---

## CROSS-REFERENCE: PACKAGES BY STATUS

**🔴 HAS REAL BUGS (fix before production):**
api (rest.go getInt/getSlice type assertions), pkg/node (founderPub not passed to SecuritySubsystem), pkg/integration (AssignRole doesn't store, wrong delete key), pkg/orchestrator (dead session_manager.go)

**⚠️ MINOR ISSUES (should fix but not blocking):**
pkg/eventbus (TOCTOU race in Publish/Stop), pkg/content (AddProvider overwrites), pkg/providers (model_catalog false positives), pkg/storage (no GC goroutine), pkg/policy (ABAC non-functional), pkg/security (CheckOrigin open), pkg/sandbox (command injection risk), pkg/registry (no TTL)

**✅ CORRECTLY WORKING:**
pkg/common, pkg/config, pkg/crypto, pkg/identity, pkg/search, pkg/protocol, pkg/validation, pkg/timeout, pkg/logger, pkg/ledger, pkg/mailbox, pkg/network, pkg/network/domain, pkg/delegation, pkg/naming, pkg/channel, pkg/gateway, pkg/email, pkg/hosting, pkg/session, pkg/session/core, pkg/agent, pkg/agent/adapters, pkg/agent_bridge, pkg/sdk, pkg/runtime/*, pkg/capability (sub-packages), cmd/*, api/dashboard

**🧊 ISOLATED (not imported by any entry point — packages being integrated):**
pkg/agent/unified (22 files — Cursor-like agent system), pkg/agent/* (automation, collaboration, direction, learning, memory, quality, skills, subagents, tasks, thinking, tools, tracking, validation — all isolated sub-packages), pkg/events/*, pkg/memory/*, pkg/session/advanced, pkg/session/connection, pkg/discovery, pkg/plugins

**📝 NOTE:**
Many "isolated" packages are development-phase artifacts built by Devon. They are not bugs — they represent features being integrated. The system works correctly with what's connected in cmd/studio/main.go.

---

> End of Map — This document covers all ~365 .go files across 60+ packages
